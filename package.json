{
  "name": "grunt-plugin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "rimraf .tmp && grunt && mocha test/test.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "grunt": "^1.0.1",
    "mocha": "^3.3.0",
    "rimraf": "^2.5.0"
  },
  "dependencies": {
    "marked": "^0.3.5"
  }
}
